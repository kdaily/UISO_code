# Need this for pretty colors in tables
# Run this before knitting!

md2html <- function(inputFile, outputFile) {
  require(markdown)
  markdownToHTML(inputFile, outputFile, stylesheet='style.css')
}

options(rstudio.markdownToHTML=md2html)
